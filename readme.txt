11/30/2010

Eclipse Gemini Naming Build instructions:

1. Running "mvn install" should build the module and run it's associated unit and integration tests. The environment variable that was required in the previous release (GEMINI_NAMING_HOME) is no longer needed.  

 